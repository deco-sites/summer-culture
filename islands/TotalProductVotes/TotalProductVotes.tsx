import type { Props } from "deco-sites/summer-culture/components/TotalProductVotes/TotalProductVotes.tsx";
import Component from "deco-sites/summer-culture/components/TotalProductVotes/TotalProductVotes.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
